<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>MAJAFAM | <%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link href="/css/custom.css" rel="stylesheet">
  <style>
    body { background: linear-gradient(135deg,#0f172a,#1e293b); color: #f1f5f9; font-family: 'Inter', sans-serif; padding-top: 70px; }
    .navbar { background: rgba(30, 47, 59, 0.9); backdrop-filter: blur(10px); border-bottom: 1px solid rgba(255,255,255,0.06); }
    .nav-link.active { border-bottom: 2px solid #38bdf8; font-weight: bold; color: #38bdf8 !important; }
    .dropdown-menu { background-color: #1e293b; }
    .dropdown-item { color: #f1f5f9; }
    .dropdown-item:hover { background-color: #334155; }
    .dropdown-item.active, .dropdown-item:active { background-color: #2563eb; }
  </style>
</head>
<body>
  <%- include('toast', { toast: typeof toast !== 'undefined' ? toast : null }) %>
  <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">
        <i class="bi bi-award-fill me-2 text-info"></i>MAJAFAM
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="mainNavbar">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link <%= title==='Dashboard' ? 'active' : '' %>" href="/dashboard">
              <i class="bi bi-house-door"></i> Dashboard
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= title==='Tukar Poin' ? 'active' : '' %>" href="/tukar-point">
              <i class="bi bi-arrow-left-right"></i> Tukar Poin
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= title==='Raffles' ? 'active' : '' %>" href="/raffles">
              <i class="bi bi-gift"></i> Raffles
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= title==='Tugas' ? 'active' : '' %>" href="/tasks">
              <i class="bi bi-list-check"></i> Tasks
            </a>
          </li>
        </ul>

        <ul class="navbar-nav ms-auto align-items-center">
          <% /* Tampilkan poin hanya kalau user login dan points valid */ %>
          <% if (user && !isNaN(Number(user.points))) { %>
            <li class="nav-item me-3">
              <span class="navbar-text fw-bold">
                <i class="bi bi-star-fill text-warning"></i>
                <%= Number(user.points).toLocaleString('id-ID') %> Poin
              </span>
            </li>
          <% } %>

          <li class="nav-item me-3">
            <a class="btn btn-success btn-sm" href="/tip">
              <i class="bi bi-send"></i> Tip
            </a>
          </li>

          <li class="nav-item me-3">
            <a class="nav-link position-relative" href="#">
              <i class="bi bi-bell"></i>
            </a>
          </li>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
              <i class="bi bi-person-circle"></i>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow">
              <li><span class="dropdown-item-text text-primary fw-bold"><%= user ? user.username : 'Guest' %></span></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/history"><i class="bi bi-clock-history me-2"></i> Riwayat</a></li>
              <li><a class="dropdown-item" href="/support"><i class="bi bi-headset me-2"></i> Support</a></li>
              <li><hr class="dropdown-divider"></li>
              <% if (user) { %>
                <li><a class="dropdown-item text-danger" href="/logout"><i class="bi bi-box-arrow-right me-2"></i> Logout</a></li>
              <% } else { %>
                <li><a class="dropdown-item" href="/login"><i class="bi bi-box-arrow-in-right me-2"></i> Login</a></li>
              <% } %>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>
