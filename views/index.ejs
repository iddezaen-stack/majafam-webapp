<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <title>MAJAFAM | <%= title %></title>
    <style>
        :root { --bg-dark: #0f212e; --panel-bg: #1a2c38; --text-light: #e2e8f0; --text-muted: #ffffff; --border-color: #2c3e50; --accent-green: #00e701; --accent-blue: #38bdf8; }
        html, body { height: 100%; }
        body { background-color: var(--bg-dark); color: var(--text-light); font-family: 'Inter', sans-serif; }
        .navbar { background-color: var(--panel-bg); border-bottom: 1px solid var(--border-color); }
        .btn-action { font-weight: 600; border-radius: 0.25rem; padding: 0.8rem 1.5rem; border: none; color: #0f212e; text-transform: uppercase; letter-spacing: 0.5px; transition: filter 0.2s ease; }
        .btn-action:hover { filter: brightness(1.15); }
        .btn-green-stake { background-color: var(--accent-green); }
    </style>
</head>
<body class="d-flex flex-column">
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">
    		<i class="bi bi-award-fill me-2" style="color: var(--accent-green);"></i>
    		<span style="color: var(--accent-green);">MAJAFAM</span>
	    </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"><span class="navbar-toggler-icon"></span></button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <% if (user) { %>
                        <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"><i class="bi bi-person-circle me-1"></i><%= user.username %></a>
                            <ul class="dropdown-menu dropdown-menu-dark dropdown-menu-end">
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/logout">Logout</a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/privacy-policy">Privacy Policy</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/terms">Syarat & Ketentuan</a>
                        </li>
                                                <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
                        <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container text-center d-flex flex-grow-1 align-items-center justify-content-center">
        <div>
            <% if (user) { %>
                <h1 class="display-4 fw-bold text-white">Selamat Datang Kembali, <%= user.username %>!</h1>
                <p class="col-lg-8 mx-auto fs-4 text-white">Anda siap untuk mengumpulkan lebih banyak poin?</p>
                <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mt-5">
                    <a href="/dashboard" class="btn btn-action btn-green-stake btn-lg px-4">Buka Dashboard</a>
                </div>
            <% } else { %>
                <h1 class="display-4 fw-bold text-primary">Platform Loyalitas YouTube Point oleh MAJAFAM</h1>
                <p class="col-lg-8 mx-auto fs-4 text-white">Kumpulkan poin dengan menonton dan berinteraksi aktif di live stream YouTube resmi kami.</p>
                
                                <p class="col-lg-8 mx-auto fs-6 text-white-50">
                  Proses login Google memerlukan izin READ-ONLY untuk memverifikasi Channel ID Anda, yang digunakan 
                  untuk melacak interaksi chat Anda demi tujuan alokasi poin. Data tidak digunakan untuk tujuan lain.
                </p>
                
                <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mt-5">
                    <a href="/login" class="btn btn-action btn-green-stake btn-lg px-4">Masuk</a>
                    <a href="/register" class="btn btn-outline-light btn-lg px-4">Daftar Sekarang</a>
                </div>
            <% } %>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
